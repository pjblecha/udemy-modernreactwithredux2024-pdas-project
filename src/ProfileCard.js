function ProfileCard({title, handle}) {
    return (
        <div>
            <div>Title: {title}</div>
            <div>Twitter Handle: {handle}</div>
        </div>
    );
}

export default ProfileCard;